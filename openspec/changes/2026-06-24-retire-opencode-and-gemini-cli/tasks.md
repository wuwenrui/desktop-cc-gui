# Tasks: 客户端下线 OpenCode / Gemini CLI 引擎 (retire-opencode-and-gemini-cli)

## 2026-06-27 Incremental Provider Settings UI Slice

- [x] P0 [I: `src/features/vendors/components/VendorSettingsPanel.tsx`, `src/features/vendors/components/GeminiVendorPanel.tsx`, `src/styles/settings.part1.vendor-panels.css`, `src/features/vendors/types.ts`, `src/i18n/locales/{en,zh}.part1.ts`] [O: 删除供应商管理页 Gemini CLI tab、Gemini provider settings panel 与 `vendor-gemini-*` dead CSS; 供应商描述收敛为 Claude/Codex; 保留 `useGeminiVendorManagement`、`services/tauri/vendors.ts` Gemini bridge 与 Rust backend vendor command 作为兼容层] [V: `node node_modules/vitest/vitest.mjs run --maxWorkers 1 --minWorkers 1 src/features/vendors/components/VendorSettingsPanel.test.tsx`; `npm run typecheck`; `npm run check:large-files`; `git diff --check`] 增量移除 Gemini CLI 供应商配置前端 UI。

## 0. Baseline And Preflight

- [ ] 0.1 [P0][depends:none][I: 当前 4-engine 状态 + `src-tauri/src/engine/*.rs` 文件清单 + `src/features/opencode/**` 完整文件清单 + `src/services/tauri/appServer.ts` / `runtimeMode.ts` / `src/types.ts:760` / `src-tauri/src/types.rs:909` / `src-tauri/src/vendors/commands.rs` / `src-tauri/src/local_usage/gemini_sessions.rs` / `src-tauri/src/bin/cc_gui_daemon.rs:753` / `daemon_state.rs:1090` / `engine_bridge.rs` / `src/features/threads/loaders/opencodeHistoryLoader.ts` / `src/features/engine/components/EngineSelector.tsx` / `EngineIcon.tsx` / `useEngineController.ts:394-877` / `src/features/settings/components/settings-view/sections/CodexSection.tsx:1297-1301` / `useAppSettings.ts:141,321` / `src/styles/opencode-panel.css` / `composer.part1.css` / `sidebar.css` / `src/i18n/locales/{en,zh}.part{1..6}.ts` / `src/features/context-ledger/pricing/fixtures/opencode.ts` / `scripts/check-engine-capability-matrix.mjs` / `scripts/scan-engine-name-branches.mjs`][O: 落 `docs/retire/v0.5.14-baseline.md`, 内容必须含 6 段: (1) `rg -n "opencode|gemini_cli|gemini-cli|geminiCli" src src-tauri/src | wc -l` 当前命中行数 1395 (来自本次扫描), 拆分为前端/后端/CSS/i18n/test 5 类分别多少行; (2) 4 engine 各自的 capability matrix 行 + fixture 文件行数; (3) 老 .jsonl 在 `~/.ccgui/<provider>/threads/` 目录数量 (开发机实测, 不是估算); (4) `AppSettings.opencodeEnabled` 在源码中出现位置全清单 (行号); (5) vendors/commands.rs 唯一调用点确认 (跑 `rg -n "vendors::commands::" src-tauri/src | grep -v vendors/commands.rs`, 必须 0 命中, 否则阻塞 task 7); (6) `selectedAgentSession.resolveThreadEngine` 当前所有 `startsWith` 分支 + 兜底缺口][V: 文件存在, 6 段均非空且与本次扫描一致, vendors 调用点 0 命中, `git diff` 显示真实代码引用] 落 baseline, 给后面"不回归"硬门禁留证据。
- [ ] 0.2 [P0][depends:0.1][I: 当前 `src-tauri/src/engine/{mod.rs, opencode.rs, commands_opencode*.rs, gemini*.rs}` 文件 + 互 import 关系][O: 画依赖图 `docs/retire/v0.5.14-engine-import-graph.dot`, 用 graphviz 语法, 节点 = 8 个 engine 文件 + `mod.rs` + `commands.rs` + `events.rs` + `manager.rs` + `status.rs` + `vendors/commands.rs` + `daemon/engine_bridge.rs` + `daemon_state.rs`, 边 = `use crate::engine::xxx` 关系; 验证 `commands_opencode_helpers` 只被 `commands_opencode` import, `gemini_event_parsing` 只被 `gemini` / `gemini_history` import, 等等 — 任何"跨 gemini* 互引但外部模块不引"的边用虚线标出, 证明整模块拆出安全][V: 图存在, 整模块拆出后 `cargo check` 应该 0 编译错误 (除 dangling 之外)] 画 engine 内部 import 图。
- [ ] 0.3 [P0][depends:0.1][I: `src/features/opencode/**` 完整清单 + 全文搜 import][O: 跑 `rg -n "from.*features/opencode" src` + `rg -n 'from "\.\./opencode' src` + `rg -n 'import.*opencode' src` 三条命令, 落 `docs/retire/v0.5.14-opencode-importers.md`, 列每个 import 点 (文件:行) + 该文件是否在本次删除范围; 整 feature folder 拆出后这些 import 全部 must be resolved][V: 文件存在, 全部命中点都有 follow-up 归属 (删 / 改 / 保留)] 列出 opencode feature folder 的所有 import 点。
- [ ] 0.4 [P0][depends:0.1][I: `src-tauri/src/vendors/commands.rs` gemini-only handler/helper + `command_registry.rs` + `src/services/tauri/vendors.ts`][O: 跑 `rg -n "vendor_.*gemini|GeminiVendor|gemini_preflight|normalize_gemini_auth_mode|default_gemini_auth_mode" src-tauri/src src/services`, 落 `docs/retire/v0.5.14-vendors-gemini-callers.md`; 显式标注哪些命中删、哪些属于历史 docs/spec 不动; 同时记录 `vendors/commands.rs` 中 Claude/Codex provider IPC 保留清单][V: 文件存在, 每个 gemini-only 命中点都有迁移/删除归属, Claude/Codex vendor handler 清单非空] vendors gemini-only 调用点审计。
- [ ] 0.5 [P0][depends:0.1][I: `src/styles/opencode-panel.css` + `composer.part1.css` + `sidebar.css`][O: 跑 `rg -n "opencode" src/styles/composer.part1.css` + `rg -n "opencode" src/styles/sidebar.css`, 落 `docs/retire/v0.5.14-css-orphans.md` 列出具体选择器行 + 行数 (~30+20 ≈ 50 行), 删后跑 `npm run lint:css` (如有) 确认无 dangling rule][V: 文件存在, 行数与扫描一致] CSS 孤儿选择器清单。
- [ ] 0.6 [P0][depends:0.1][I: `src/i18n/locales/{en,zh}.part{1..6}.ts`][O: 跑 `rg -n "opencode:|gemini:" src/i18n/locales/ | wc -l` 当前命中行数 + `npm run test -- canvasCopy.snapshot` 当前 snapshot 行数, 落 `docs/retire/v0.5.14-i18n-snapshot.md` 记 (1) 删多少 key (2) snapshot 期望新行数 (3) 是否需要手动 regenerate][V: 文件存在, snapshot 期望值 = 当前 - 删的 key 数] i18n 段影响 + snapshot 期望。

## 1. Type / EngineType 层 (Stage 1, 低风险)

- [ ] 1.1 [P0][depends:0.1][I: `src-tauri/src/engine/mod.rs` 当前 `EngineType` 定义 + `serde(rename_all = "...")`][O: 删 `EngineType::OpenCode` / `EngineType::Gemini` 变体; 保留 `Codex` / `Claude`; 自定义 `Deserialize` 兜底: 遇到 `"opencode"` / `"gemini"` / 任何未知字符串 → 返回 `EngineType::Codex` + 调 `tracing::warn!("EngineType::deserialize: unknown engine '{}', falling back to Codex", s)`; 加 `#[serde(rename_all = "lowercase")]` 保留; 配套单测 `engine_legacy_deserialize_test.rs` 3 case: `"opencode"` → `Codex`, `"gemini"` → `Codex`, `"foo"` → `Codex` + warn 触发 1 次][V: `cargo test --manifest-path src-tauri/Cargo.toml engine_legacy_deserialize` 全 pass, `cargo check` 0] EngineType 收敛 + 反序列化兜底。
- [ ] 1.2 [P0][depends:1.1][I: `src-tauri/src/types.rs:909` + 同文件 `geminiEnabled` 字段 + `src/features/settings/hooks/useAppSettings.ts:141, 321` + `src/types.ts:760`][O: 改 `AppSettings.opencode_enabled` 字段: 加 `#[allow(dead_code)]` + 注释 `// deprecated since v0.5.14, always false, see engine-deprecation spec`, `serde` 行为不变 (保留 `rename = "opencodeEnabled"`); 改 `useAppSettings.ts:141` 默认值 `opencodeEnabled: false` (已是); 改 `:321` setter 永远写 `opencodeEnabled: false` (无论用户传什么); 同样处理 `geminiEnabled`; Rust 端启动时如果 `opencode_enabled == true`, 调 `tracing::warn!("AppSettings.opencodeEnabled is deprecated and ignored since v0.5.14")` 不报错][V: `cargo check` 0, `npm run test -- useAppSettings` 全 pass, 老 config 反序列化通过] 字段冻结 + setter 永远 false。
- [ ] 1.3 [P0][depends:1.1][I: `src-tauri/src/session_management_types.rs` `SessionKind` 枚举定义][O: 删 `SessionKind::OpenCode` / `SessionKind::Gemini` 变体, 保留 `Codex` / `Claude`; 自定义 `Deserialize` 兜底逻辑同 Decision 4; 加 `SessionKind::legacy_from_str` 兼容函数; 单测覆盖未知字符串 → `Codex` + warn][V: `cargo test SessionKind` 全 pass, `cargo check` 0] SessionKind 收敛。
- [ ] 1.4 [P0][depends:1.1, 1.2][I: `src/features/engine/components/EngineIcon.tsx` `engine` case + `EngineIcon.test.tsx` 断言][O: 删 `case "opencode":` / `case "gemini":` 分支; 删 `EngineIcon.test.tsx` 中对应断言; EngineType 联合类型从 `"claude" | "codex" | "opencode" | "gemini"` 收敛到 `"claude" | "codex"`][V: `npm run test -- EngineIcon` 全 pass, `npm run typecheck` 0] EngineIcon 收敛。
- [ ] 1.5 [P0][depends:1.1][I: `src/features/engine/components/EngineSelector.tsx` + `src/features/engine/hooks/useEngineController.ts:394-877`][O: EngineSelector 下拉删除 opencode / gemini 选项; `useEngineController` 删 `geminiEnabled` / `opencodeEnabled` 派生逻辑 (line 394, 402, 406, 877); `enabledEngines` 类型从 `{claude, codex, opencode, gemini}` 收敛到 `{claude, codex}`; 现有 `useEngineController.test.tsx` 对应测试用例删][V: `npm run test -- useEngineController EngineSelector` 全 pass, `npm run typecheck` 0] EngineSelector 收敛 + useEngineController 派生逻辑简化。

## 2. 后端 engine 整模块拆出 (Stage 2, 中风险)

- [ ] 2.1 [P0][depends:1.1][I: `src-tauri/src/engine/opencode.rs` (1809 行) + `commands_opencode.rs` (931) + `commands_opencode_helpers.rs` (140) + `mod.rs` 8 行 `pub mod` 声明][O: `git rm src-tauri/src/engine/opencode.rs src-tauri/src/engine/commands_opencode.rs src-tauri/src/engine/commands_opencode_helpers.rs`; 改 `src-tauri/src/engine/mod.rs` 删 3 行 `pub mod opencode;` / `pub mod commands_opencode;` / `pub mod commands_opencode_helpers;`; 跑 `cargo check` 0 (如果 break, 见 task 2.4 fallback)][V: `cargo check` 0 退出码, 文件不存在] opencode 后端 3 文件拆出。
- [ ] 2.2 [P0][depends:2.1][I: `src-tauri/src/engine/gemini.rs` (1412) + `gemini_event_parsing.rs` (911) + `gemini_history.rs` (1695) + `gemini_proxy_guard.rs` (139) + `gemini_tests.rs` (856) + `mod.rs` 5 行声明][O: `git rm` 5 文件; 改 `mod.rs` 删 5 行 `pub mod gemini*;`; 跑 `cargo check` 0][V: `cargo check` 0, 5 文件不存在] gemini 后端 5 文件拆出。
- [ ] 2.3 [P0][depends:2.1, 2.2][I: `src-tauri/src/engine/{claude_forwarder.rs, commands.rs, commands_tests.rs, commands_parse_helpers.rs, events.rs, manager.rs, status.rs, capability_matrix.rs}` 中 opencode / gemini 分支][O: 删这 8 个文件里所有 `match engine { EngineType::OpenCode => ... EngineType::Gemini => ... }` 分支; 收敛到 2 路 `Codex` / `Claude`; `capability_matrix.rs` 删 2 个 engine 的 capability 描述行; 加 `capability_matrix_test` 6 个 engine (Codex 3 + Claude 3) 全 pass][V: `cargo test capability_matrix` 6 个全 pass, `cargo check` 0, `cargo build --release` 0] engine 基础模块分支收敛。
- [ ] 2.4 [P0][depends:2.1, 2.2][I: 任何 cargo check 报错位置][O: 对每个 `error[E0432): unresolved import 'crate::engine::opencode'` / `'crate::engine::gemini*'` 报错, 改对应 upstream 文件 (commands.rs / manager.rs / status.rs / events.rs) 删 import 引用 + 删 match 分支; 禁止留 `#[allow(unused_imports)]` / `#[allow(dead_code)]` 兜底, 必须真删][V: `cargo check` 最终 0, 无 `#[allow(...)]` 兜底残留 (跑 `rg -n 'allow\(unused_imports|dead_code\)' src-tauri/src/engine/` 命中 0)] 编译错误逐个修复。
- [ ] 2.5 [P0][depends:0.4, 2.3][I: `src-tauri/src/vendors/commands.rs` gemini-only 子段 + `command_registry.rs` vendor gemini 注册 + `src/services/tauri/vendors.ts`][O: 保留 `src-tauri/src/vendors/commands.rs` 文件与 Claude/Codex provider IPC; 仅删除 `vendor_get_gemini_settings` / `vendor_save_gemini_settings` / `vendor_gemini_preflight` 3 个 handler、`GeminiVendor*` schema、gemini-only helper、`command_registry.rs` 3 个注册项、前端 3 个 client IPC 包装; 跑 `cargo check` 0; 跑 `rg -n "vendor_.*gemini|GeminiVendor|gemini_preflight" src-tauri/src src/services` 命中 0 (除历史 docs/spec 之外)][V: `cargo check` 0, vendors/commands.rs 仍存在, Claude/Codex vendor tests 通过, gemini-only vendor 符号 0 命中] vendors gemini-only proxy 拆出。
- [ ] 2.6 [P0][depends:2.3][I: `src-tauri/src/bin/cc_gui_daemon.rs:753` + `daemon_state.rs:1090` + `engine_bridge.rs`][O: 删 3 文件里 `opencode` / `gemini` 启动分支 + `opencode:<session_id>` 拼装; 收敛到 2 engine; raw string → engine 解析边界遇到未知/legacy engine 时 fallback 到 `Codex` 并记录原始字符串 warn; `engine_bridge` 收到的 typed `EngineType` 只覆盖 Codex/Claude 两个 dispatch path; 加 daemon test 覆盖 2 个 engine 启动 + 1 个 raw unknown fallback case][V: `cargo test cc_gui_daemon::engine_bridge` 全 pass, `cargo check` 0] daemon engine 启动分支收敛。

## 3. 后端 session / local_usage 软下线 (Stage 3, 低风险)

- [ ] 3.1 [P0][depends:2.2][I: `src-tauri/src/local_usage/gemini_sessions.rs` (508 行) + `src-tauri/src/local_usage.rs` `pub mod gemini_sessions;` + 调用点][O: 改 `gemini_sessions.rs` 内部所有 `pub fn` 函数体为 no-op: `list_local_gemini_sessions` 返回 `Vec::new()` + `tracing::warn!`; `delete_gemini_sessions` 返回 `Ok(0)` + warn; 其它 helper 同理; 文件保留不删; 顶部加注释 `// deprecated since v0.5.14, scheduled removal v0.5.15`; 跑 `cargo check` 0][V: `cargo check` 0, `rg -n "gemini_sessions::" src-tauri/src | grep -v 'src-tauri/src/local_usage/gemini_sessions.rs'` 命中 0 表示无其它调用点, 或有但都通过 no-op 不破坏] gemini_sessions 软下线。
- [ ] 3.2 [P0][depends:2.3][I: `src-tauri/src/local_usage/session_delete.rs` (391) + `local_usage.rs` `pub mod session_delete;` + 调用点][O: 删 `session_delete.rs` 中 `EngineType::Gemini` / `EngineType::OpenCode` 分支; 收敛到 2 engine; 调用 `gemini_sessions::delete_*` 改成 `tracing::warn!` 占位 (因为 stage 3.1 已经 no-op)][V: `cargo test local_usage` 全 pass, `cargo check` 0] session_delete 分支收敛。
- [ ] 3.3 [P0][depends:1.3, 2.3][I: `src-tauri/src/session_management.rs` + `_types.rs` + `_catalog_projection.rs` + `_archive_evidence.rs` + `_tests.rs` + `_archive_delete_tests.rs`][O: 删 6 文件里 `SessionKind::OpenCode` / `SessionKind::Gemini` 分支 (因为 stage 1.3 SessionKind 已收敛); 收敛到 `Codex` / `Claude`; `SessionKind::OpenCode` 的 catalog tag 改为 `Codex` 兜底 + warn (或整个删, 看 catalog source enum 怎么定义); 加 session_management_test 覆盖 2 engine catalog projection 行为不变][V: `cargo test session_management` 全 pass, `cargo check` 0] session_management 全链路收敛。

## 4. 前端 feature folder 整拆出 (Stage 4, 中风险)

- [ ] 4.1 [P0][depends:0.3][I: `src/features/opencode/components/*.tsx` (6 文件 1525 行) + `store/*.ts` (2 文件 74 行) + `hooks/useOpenCodeControlPanel.ts` (333) + `types/index.ts` (44) + `*test*` 2 文件][O: `git rm -r src/features/opencode`; 跑 `npm run typecheck` 0 (task 0.3 的 importer 列表已全部 follow-up); 跑 `npm run test` 0][V: `npm run typecheck` 0, `npm run test` 0, `src/features/opencode/` 目录不存在] opencode 前端 folder 整拆出。
- [ ] 4.2 [P0][depends:4.1][I: 4.1 触发 typecheck 报错的所有 import 点 (按 0.3 importer 列表)][O: 对每个 import 报错 (e.g. `useOpenCodeControlPanel` 被 import 处), 删该 import 句 + 删对应调用代码块; 禁止留 `// @ts-expect-error` / `// @ts-ignore` 兜底; 如果调用代码块本身只服务于 opencode, 整段删][V: `npm run typecheck` 0, `rg -n "opencode" src/ | grep -v 'opencode-history\|opencode-engine' specs` (应该 0 命中生产代码, 除软下线文件外)] opencode importer 全部 follow-up。

## 5. App shell / Composer / Settings / i18n / CSS (Stage 5, 中风险)

- [ ] 5.1 [P0][depends:1.5, 4.2][I: `src/app-shell.tsx:559, 579` + `src/app-shell-parts/useAppShellLayoutNodesSection.tsx:677, 1201` + `useAppShellKanbanComposerSection.ts:171` + `useAppShellKanbanExecutionSection.ts:663-923` + `useAppShellWorkspaceFlowsSection.ts` + `useAppShellSections.kanbanHelpers.ts`][O: 删 6 文件里 `opencode` / `gemini` engine 字符串 + `opencode-pending-*` 前缀判断; 收敛 engine 联合类型到 `"claude" | "codex"`][V: `npm run typecheck` 0, `npm run test` 0] app-shell-parts 分支收敛。
- [ ] 5.2 [P0][depends:5.1][I: `src/app-shell-parts/manualThreadRecovery.ts:1, 72-73` + `selectedAgentSession.ts:60, 67-68` + `useCreateSessionLoading.ts:64` + `useOpenCodeSelection.ts` (含同名 test) + `useOpenCodeSelection.test.ts`][O: 删除 active opencode/gemini engine 分支; `git rm src/app-shell-parts/useOpenCodeSelection.ts src/app-shell-parts/useOpenCodeSelection.test.ts`; `ManualRecoveryEngine` 类型从 `"claude" | "codex" | "gemini" | "opencode"` 收敛到 `"claude" | "codex"`; `selectedAgentSession.resolveThreadEngine` 保留 legacy fallback: `opencode:` / `opencode-pending-` / `gemini:` → `"codex"`; 加 `selectedAgentSession_test` 3 个 case 验证这三个 legacy prefix][V: `npm run test -- selectedAgentSession useOpenCodeSelection` 全 pass, `npm run typecheck` 0] app-shell-parts 兜底分支 + useOpenCodeSelection 整文件拆出。
- [ ] 5.3 [P0][depends:5.1][I: `src/features/composer/components/ChatInputBox/{selectors/ModelSelect.tsx, selectors/ProviderSelect.tsx, ChatInputBoxAdapter.tsx, ChatInputBoxAdapter.test.tsx, Composer.tsx, ComposerInput.tsx, PromptEnhancerDialog.tsx, hooks/usePromptEnhancer.ts, hooks/usePromptEnhancer.test.tsx, types.ts, modelOptions.ts, modelOptions.test.ts}` + `src/features/composer/utils/composerFileReferences.ts`][O: 删 13 文件里 `engine === "opencode"` / `engine === "gemini"` 分支; 删 modelOptions.ts 中 opencode / gemini model 列表; 删 ProviderSelect.tsx 中 opencode / gemini provider option; `types.ts` 联合类型收敛到 `"claude" | "codex"`; `modelOptions.test.ts` 删对应 fixture][V: `npm run test -- ChatInputBox Composer modelOptions ProviderSelect ModelSelect usePromptEnhancer` 全 pass, `npm run typecheck` 0] ChatInputBox + Composer 全链路分支收敛。
- [ ] 5.4 [P0][depends:1.2][I: `src/features/settings/components/settings-view/sections/CodexSection.tsx:1297-1301` + `CodexSection.test.tsx:45` + `src/features/settings/components/SettingsView.test.tsx:213, 1080`][O: 删 `CodexSection.tsx` 中 OpenCode toggle UI (line 1297-1301) + `onChange` handler 调 `updateSettings({...prev, opencodeEnabled: checked})` 改成不调 / 调但无效; 删 2 个 test 文件中 `opencodeEnabled: true` fixture; 保留 `useAppSettings.test.ts:81, 371, 381, 388` 改成断言 `opencodeEnabled: false` 永远][V: `npm run test -- CodexSection SettingsView useAppSettings` 全 pass, `npm run typecheck` 0] Settings UI 移除 opencode toggle。
- [ ] 5.5 [P0][depends:0.5][I: `src/styles/opencode-panel.css` (915) + `composer.part1.css` 中 ~30 行 opencode 选择器 + `sidebar.css` 中 ~20 行][O: `git rm src/styles/opencode-panel.css`; 改 `composer.part1.css` 删 `.composer-opencode-model-*` 5 段 + `.is-opencode-model-picker`; 改 `sidebar.css` 删 opencode 相关选择器 (按 0.5 baseline); 跑 `npm run lint:css` (如有) 0; 跑 `npm run typecheck` 0 (CSS 不影响 TS, 但确认无 .d.ts 引用)][V: `npm run lint:css` 0, 3 文件按 baseline 修改, `rg -n "opencode" src/styles/` 命中 0] CSS 收敛。
- [ ] 5.6 [P0][depends:0.6][I: `src/i18n/locales/{en,zh}.part{1..6}.ts` 中 `opencode:` / `gemini:` 段 + `src/i18n/locales/canvasCopy.snapshot.test.ts`][O: 删 12 个 part 文件中所有 `opencode: { ... }` / `gemini: { ... }` 整段; 跑 `npm run test -- canvasCopy.snapshot` → 失败 → 跑 `npm run test -- canvasCopy.snapshot -u` regenerate snapshot → 再跑 0; 验证 snapshot 行数 = baseline - 删的 key 数 (按 0.6 期望)][V: `npm run test` 0, snapshot 期望行数匹配, `rg -n "opencode:|gemini:" src/i18n/locales/` 命中 0] i18n 段删 + snapshot 同步。

## 6. Thread / Sidebar / Spec hub / Commands / Context ledger (Stage 6, 高风险)

- [ ] 6.1 [P0][depends:5.2][I: `src/features/threads/loaders/opencodeHistoryLoader.ts` + `historyLoaders.fallbacks.test.ts` + `useThreadActions.historyLoaderFactory.ts` + `useThreadActions.opencode-timeout-fallback.test.tsx` + `useThreadActions.start-fork.test.tsx` + `useThreadActions.shared-native-compat.test.tsx`][O: 改 `opencodeHistoryLoader.ts` 函数体 no-op: `export async function loadOpencodeHistory(): Promise<Thread[]> { if (import.meta.env.DEV) console.warn('opencodeHistoryLoader: deprecated since v0.5.14, returning empty'); return []; }` 保留 export 签名; `historyLoaderFactory.ts` 中 opencode 分支保留 (调 no-op); 删 `useThreadActions.opencode-timeout-fallback.test.tsx` 整文件; 删 `start-fork.test.tsx` / `shared-native-compat.test.tsx` 中 opencode 专属用例; 改 `historyLoaders.fallbacks.test.ts` 中 opencode fallback 断言 = 返回 `[]`][V: `npm run test -- useThreadActions historyLoaders` 全 pass, `npm run typecheck` 0] opencodeHistoryLoader 软下线 + opencode-specific 测试拆出。
- [ ] 6.2 [P0][depends:6.1][I: `src/features/threads/hooks/useThreadActions.{ts, helpers.ts, sessionActions.ts, lastGoodSnapshots.ts, native-session-bridges.test.tsx, workspacePath.ts}` + `useThreadActionsSessionRuntime.ts` + `useThreadActionsResumeThread.ts` + `useThreadEventHandlers.ts` + `useThreadItemEvents.ts` + `useThreadMessaging*.ts` + `useThreadRealtimeHistoryReconcile.ts` + `useThreadTurnEvents.ts` + `useThreadTurnSettlementReconciliation.ts` (≈20 文件)][O: 删 20 文件里 `EngineType::OpenCode` / `EngineType::Gemini` / `engine === "opencode"` / `engine === "gemini"` 分支; thread 联合类型从 4 收敛到 2; `resolveThreadEngine` 调用点 1 个保留兜底 (按 Decision 6), 其余全部改成直接 `codex` / `claude` 判断; 配套 test 删对应断言][V: `npm run test -- useThreadActions useThreadMessaging useThreadTurnEvents useThreads` 全 pass, `npm run typecheck` 0] useThread* hooks 收敛。
- [ ] 6.3 [P0][depends:6.1][I: `src/features/threads/hooks/useThreads*.ts` (8 文件含 2 test) + `useThreadsReducer*.ts` (5 文件含 2 test) + `utils/*` (≈15 文件)][O: 同 6.2, 删 28 文件里 opencode / gemini 分支; engine-source 联合类型收敛; test 删 opencode / gemini fixture; `useThreads.engine-source.test.tsx` / `useThreadsReducer.engine-source.test.ts` 重写只剩 codex / claude 双 engine source 行为][V: `npm run test -- useThreads useThreadsReducer threadStorage threadDelete sidebarSnapshot conversationCompletionEmail` 全 pass, `npm run typecheck` 0] useThreads* / useThreadsReducer* / utils 收敛。
- [ ] 6.4 [P0][depends:5.2][I: `src/features/app/components/{PinnedThreadList, Sidebar, ThreadList, sidebarInternals}.tsx` + `src/features/app/hooks/{useAppServerEvents.ts, useAppServerEvents.test.tsx, useGitCommitController.test.tsx, useSidebarMenus.ts, useSidebarMenus.test.tsx, useWorkspaceActions.ts, useWorkspaceActions.test.tsx}` + `src/features/workspaces/components/WorkspaceHome.tsx` (≈10 文件)][O: 删 10 文件里 `engine === "opencode"` / `engine === "gemini"` 分支 + opencode / gemini 引擎徽标; Sidebar / ThreadList 不再显示这两类 thread; WorkspaceHome 不再显示这两类 engine 卡片; test 删对应 fixture][V: `npm run test -- Sidebar PinnedThreadList ThreadList useAppServerEvents useGitCommitController useSidebarMenus useWorkspaceActions WorkspaceHome` 全 pass, `npm run typecheck` 0] Sidebar / Workspace / Hooks 收敛。
- [ ] 6.5 [P0][depends:5.2][I: `src/features/spec/hooks/useSpecHub.ts` + `useSpecHub.test.tsx` + `src/features/spec/components/SpecHub.test.tsx` + `src/features/spec/components/spec-hub/presentational/SpecHubPresentationalImpl.tsx` + `src/features/spec/components/SpecHub.test-support.tsx` + `src/lib/spec-core/types.ts`][O: 删 6 文件里 opencode / gemini engine 适配; `spec-core/types.ts` 中 engine 联合类型从 4 收敛到 2; test fixture 删 opencode / gemini][V: `npm run test -- useSpecHub SpecHub` 全 pass, `npm run typecheck` 0] Spec Hub 收敛。
- [ ] 6.6 [P0][depends:5.2][I: `src/features/commands/hooks/useCustomCommands.ts` + `useCustomCommands.test.tsx`][O: 删 2 文件里 opencode / gemini command 分支; test 删对应 fixture][V: `npm run test -- useCustomCommands` 全 pass, `npm run typecheck` 0] Custom commands 收敛。
- [ ] 6.7 [P0][depends:5.2][I: `src/features/context-ledger/cost/{costAggregate, costHistoryStore, costProjection.test}.ts` + `pricing/{fixtures/opencode, pricingRegistry, pricingRegistry.test}.ts` + `utils/contextLedgerProjection.test.ts`][O: `git rm src/features/context-ledger/pricing/fixtures/opencode.ts`; 改 `pricingRegistry.ts` 循环注册 2 engine (Codex + Claude); 删 4 个 test 文件中 opencode / gemini fixture + 断言; costAggregate / costHistoryStore 删对应 engine 路径][V: `npm run test -- contextLedgerProjection costProjection pricingRegistry` 全 pass, `npm run typecheck` 0] Context ledger 收敛。
- [ ] 6.8 [P0][depends:5.3][I: `src/services/tauri/appServer.ts` + `runtimeMode.ts` + `tauri.test.ts` + `src/services/globalRuntimeNotices.ts:40, 104` + `src/services/tauri/vendors.ts` (如有) + `src/services/listenerOwners.ts` (如有)][O: 删 4 文件里 opencode / gemini IPC 方法 + engine 类型; `runtimeMode.ts` 中 opencode / gemini 入口删; globalRuntimeNotices.ts 中 `case "opencode":` / `case "gemini":` 删; tauri.test.ts 删对应 mock][V: `npm run test -- tauri globalRuntimeNotices` 全 pass, `npm run typecheck` 0] Tauri IPC + Services 收敛。
- [ ] 6.9 [P0][depends:5.2][I: `src/app-shell-parts/modelSelection.test.ts` + `selectedAgentSession.flow.test.ts` + `selectedComposerSession.ts`][O: 删 3 文件里 opencode / gemini 断言 + 分支; `selectedComposerSession.ts` 删对应 resolveThreadEngine 调用][V: `npm run test -- modelSelection selectedAgentSession selectedComposerSession` 全 pass, `npm run typecheck` 0] App shell model selection 收敛。
- [ ] 6.10 [P0][depends:0.1, 6.8][I: `src/services/tauri/{appServer.ts, runtimeMode.ts, session.ts, tauri.test.ts, vendors.ts, agents.ts, cliInstaller.ts}` + `src/bootstrap.ts` + `src/types.ts` (非 AppSettings 部分)][O: 删 8 文件里 opencode / gemini 引用; bootstrap.ts 删 `import "./styles/opencode-panel.css";`; types.ts 中 union type 收敛][V: `npm run test` 0, `npm run typecheck` 0, `rg -n "opencode" src/services/ src/bootstrap.ts src/types.ts` 命中 0] Service + bootstrap 收敛。

## 7. capability_matrix + scan scripts (Stage 7, 中风险)

- [ ] 7.1 [P0][depends:2.3, 6.5][I: `src-tauri/src/engine/capability_matrix.rs` + `src/features/engine/engineCapabilityMatrix.test.ts` + `src/features/engine/utils/engineAvailability.ts` + `src/features/engine/utils/engineLabels.test.ts`][O: capability_matrix.rs 删 OpenCode / Gemini 2 个 engine 行; 保留 Codex / Claude; engineAvailability.ts + engineLabels.test.ts 删 opencode / gemini label; engineCapabilityMatrix.test.ts 删对应断言][V: `cargo test capability_matrix` 0, `npm run test -- engineCapabilityMatrix engineAvailability engineLabels EngineIcon` 全 pass] capability_matrix 收敛。
- [ ] 7.2 [P0][depends:7.1][I: `scripts/check-engine-capability-matrix.mjs` + `scripts/scan-engine-name-branches.{mjs,test.mjs}`][O: check-engine-capability-matrix.mjs 删 OpenCode / Gemini fixture 行; scan-engine-name-branches 硬编码数组改成 `["codex", "claude"]`; scan test 重写只剩 2 engine 验证][V: `node scripts/check-engine-capability-matrix.mjs` 0, `node scripts/scan-engine-name-branches.mjs` 0, `node scripts/scan-engine-name-branches.test.mjs` 全 pass] scripts 收敛。

## 8. 不回归 gate (Stage 8, 高风险)

- [ ] 8.1 [P0][depends:0.1..7.2][I: proposal Gate 1-3 + 全 stage 1-7 完成的代码状态][O: 跑 `openspec validate 2026-06-24-retire-opencode-and-gemini-cli --strict --no-interactive` 退出码 0; 跑 `npm run typecheck && npm run lint && npm run test` 退出码 0; 跑 `cargo check --manifest-path src-tauri/Cargo.toml && cargo build --release --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml` 退出码 0; 跑 `npm run check:runtime-contracts && npm run check:large-files && npm run check:engine-capability-matrix` 退出码 0; 跑 `node scripts/scan-engine-name-branches.mjs` 0 命中 (除 codex / claude)] 跑 Gate 1 全套。
- [ ] 8.2 [P0][depends:8.1][I: 当前 e2e 测试 / 现有 Codex / Claude engine 集成测试 + 现有 6 个 capability matrix case (Codex 3 + Claude 3)][O: 跑 Codex e2e (创建 session / 发送 / 接收 / 中断 / fork / rewind) 6 场景全 pass; 跑 Claude e2e 6 场景全 pass; 跑 `cargo test capability_matrix` 6 case 全 pass; 跑 `daemon skills_list` 4 source 测试全 pass; 跑 `app_server_cli` mock CLI 抓 argv 测试不变; 跑 `claude.rs::build_command` mock CLI 抓 argv 测试不变][V: 6+6 e2e + 6 matrix + 4 skills + 2 mock = 24 个 case 全 pass, 录屏或日志留档 `docs/retire/v0.5.14-e2e-evidence.md`] 跑 Gate 3 不回归全套。
- [ ] 8.3 [P0][depends:8.1][I: Gate 4 软下线证据 + Gate 5 release pipeline][O: (1) 启动 dev 模式 → console 看到 `gemini_sessions: deprecated` + `opencodeHistoryLoader: deprecated` 各 1 次; (2) 老 config `opencodeEnabled: true` 启动不报错, 字段被忽略; (3) `find ~/.ccgui/<provider>/threads -name 'opencode-*' -o -name 'gemini-*'` 文件**仍然存在** (软下线, 不删盘); (4) `npm run tauri:dev` macos aarch64 启动后 4 步用户路径全 pass; (5) `cargo build --release` macos aarch64 退出码 0, 产物大小比 v0.5.13 缩减 (粗估 ≥ 8%); 录屏留档 `docs/retire/v0.5.14-real-evidence.mp4`][V: 5 步全 pass, 文件保留, build 0, 产物缩 ≥ 8%, 录屏存在] 软下线 + release 证据。

## 9. Archive Gate

- [ ] 9.1 [P0][depends:8.1, 8.2, 8.3][I: 全部 task 完成 + OpenSpec 校验通过 + E2E 通过 + 不回归验证通过 + release 证据留档][O: 跑 `openspec validate 2026-06-24-retire-opencode-and-gemini-cli --strict --no-interactive` 退出码 0 → `openspec archive 2026-06-24-retire-opencode-and-gemini-cli` 同步 spec delta 到 main spec (`openspec/specs/engine-deprecation/spec.md`); 在 `openspec/project.md` 的 active changes 表里移除本 change; 在 `CHANGELOG.md` 加 v0.5.14 changelog 行 "Removed OpenCode and Gemini CLI engine; clients are now Codex/Claude dual-engine only. Historical .jsonl files are preserved on disk for rollback."; 跑 `python3 .claude/skills/osp-openspec-sync/scripts/validate-consistency.py --project-path . --full` 退出码 0][V: 4 步命令全退出码 0, `openspec/specs/engine-deprecation/spec.md` 内容已 sync, project.md 已更新, CHANGELOG.md 已写, validate-consistency 0] Archive 闭环。
