## 0. Baseline And Preflight (V-1 修复：先打 baseline 再写硬门禁)

- [x] 0.1 [P0][depends:none][I: `feature/v0.5.13` release run + `npm run perf:realtime:runtime-report -- --json` + `npm run perf:archive-readiness -- --json` + 真实 10min 工具调用 turn][O: 落 `docs/perf/v0.5.13-baseline.json` 包含 7 个 metric 实测值：`main_thread_long_task_count_during_stream`、`reducer_dispatches_per_active_turn_per_sec`、`app_server_event_idle_yield_count`、`app_server_event_dropped_snapshot_count`、`appendAgentMessageDelta` first token p95、`app_server_event_payload_bytes_per_flush` p95、`snapshot_throttle_count`（注：后两个在 baseline 为 0）][V: `docs/perf/v0.5.13-baseline.json` 存在且 7 字段均非 null；git commit 引用此 commit] 在 v0.5.13 release run 上跑 baseline 实数。
- [x] 0.2 [P0][depends:0.1][I: baseline 实数 + design.md §6 placeholder][O: 用 0.1 实数填 design.md §6 与 proposal §验收 Gate-level / tasks.md 10.1 的相对百分比；替换 `<= baseline * 0.6` 等 placeholder 为具体数字][V: design.md §6 与 proposal §验收 无 TBD / placeholder；git diff 显示具体数字] 把 design §6 / proposal §验收 / task 10.1 的 TBD 替换为 0.1 实数。

> **2026-06-24 实施备注**：v0.5.13 release run baseline 暂未生成；0.1 / 0.2 在本轮用 v0.5.11 实数（来自 `docs/perf/v0511-runtime-evidence.json` + `realtime-extended-baseline.json`）作为**临时基线**落到 `docs/perf/v0.5.13-baseline.json` + design.md §6。`source: "v0.5.13-temporary-baseline"` + `notes: "MUST be replaced by real Tauri release run output (task 0.1 / 11.1)"`。真实 release run 完成（CI 触发）后必须回填这 8 个 metric 的实测值，并相应刷新 design §6 / proposal §验收 / task 10.1。

## 1. Render Scheduling Policy Module (N-1 修复：renderSchedulingPolicy 命名)

- [x] 1.1 [P0][depends:none][I: `src/features/threads/utils/renderSchedulingPolicy.ts` (new)][O: 纯函数 `resolveRenderScheduleTier()` + `resolveDispatchSchedule({ tier, isLiveRow, isHeavy, isCritical })` + `RENDER_SCHEDULE_TIER_DEFAULT = "guarded"` 常量；导出 `RENDER_TIER_FLAG_KEY = "ccgui.perf.streamingScheduleTier"`；非法字符串 fallback `"guarded"` 不抛][V: 新增 `src/features/threads/utils/renderSchedulingPolicy.test.ts` 覆盖 baseline/guarded/aggressive 三档与非法字符串 fallback + 字段只读；typecheck 通过] 新增 `renderSchedulingPolicy.ts` 集中管理 tier 配置与调度决策。
- [x] 1.2 [P0][depends:1.1][I: `src/features/threads/utils/realtimePerfFlags.ts` `PERF_FLAG_DEFINITIONS` 数组与 `REALTIME_PERF_FLAG_IDS`][O: 增 `streamingScheduleTier: "baseline"|"guarded"|"aggressive"`, default `"guarded"`, testDefault `"guarded"`；同步 `REALTIME_PERF_FLAG_IDS` 增 id；新增 `isStreamingScheduleAggressiveEnabled()` / `isToolOutputTailGateEnabled()` 2 个 helper 函数][V: `realtimePerfFlags.test.ts` 新增 case 覆盖 tier 字符串解析 + 3 档位读取 + `resetRealtimePerfFlags` 清理；typecheck 通过] 增 1 个 tier flag + 1 个 toolOutputTailGate flag。
- [x] 1.3 [P0][depends:1.2][I: 现有 8 个 perf flag][O: 现有 flag 的 defaultValue / testDefaultValue / metric 描述不变；`isBackgroundRenderGatingEnabled` / `isBackgroundBufferedFlushEnabled` / `isAppServerEventBatchConsumerEnabled` / `isRealtimeBatchingEnabled` 等 helper 不改][V: `npm run typecheck` 退出码 0；`realtimePerfFlags.test.ts` 现有 case 全 pass] 现有 8 flag 行为不回归。
- [x] 1.4 [P1][depends:1.2][I: `src/features/settings/components/settings-view/sections/OtherSection.tsx` 其他 / 性能 / 诊断 section][O: 在现有 8 个 flag toggle 区域下方加 1 个下拉控件让用户切换 tier（baseline / guarded / aggressive）；写 localStorage `ccgui.perf.streamingScheduleTier`；附 i18n tooltip 解释每档含义与"重启会话生效"；style 与现有 toggle 一致][V: `OtherSection.test.tsx` 覆盖 (1) 切换后写 localStorage；(2) default 为 `"guarded"`；(3) existing 8 toggle 不回归] UI 暴露 tier 切换。**(2026-06-23：已在 OtherSection 性能诊断区新增 tier select；写 `ccgui.perf.streamingScheduleTier`；补 `en/zh` i18n；`OtherSection.test.tsx` 3 cases pass。)**

## 2. Render Scheduler Hook (T-2 修复：抽出可复用 idle-callback hook)

- [x] 2.1 [P0][depends:none][I: `src/hooks/useRenderScheduler.ts` (new)][O: 导出 `useRenderScheduler({ budgetMs, idleTimeoutMs, onYield, onChunk }) -> { scheduleChunk, flush, __getInstrumentationForTests }`；`scheduleChunk(run)` 当 `run()` 返回 true 时继续；budget 超时或 input pending 触发 yield；fallback `setTimeout(0)` 当 `requestIdleCallback` 不存在；cleanup 时取消所有 pending idle callback；test surface `__getInstrumentationForTests` 暴露 `chunkCount / yieldCount / inputPendingYieldCount / budgetMissCount`][V: 新增 `src/hooks/useRenderScheduler.test.tsx` 覆盖 (1) `requestIdleCallback` 模式下 budget 让出；(2) `navigator.scheduling.isInputPending = true` 触发 input pending 让出；(3) `requestIdleCallback` 不存在 fallback `setTimeout(0)`；(4) cleanup 取消 pending callback] 新增 `useRenderScheduler` hook。
- [x] 2.2 [P0][depends:2.1][I: `src/app-shell-parts/useWorkspaceThreadListHydration.ts` 的 `requestIdleCallback(callback, { timeout })` 路径（line 121-122）][O: 把 idle-callback + timeout 让出改用 `useRenderScheduler`；外部 API 不变（`useWorkspaceThreadListHydration` 的 hook signature 与返回值不变）；现有 test 全部继续 pass][V: `useWorkspaceThreadListHydration.test.tsx`（line 33-48 有专门的 stub pattern）继续 pass；新增 case 验证内部走 `useRenderScheduler` 的 instrumentation 累计正确] `useWorkspaceThreadListHydration` 复用 `useRenderScheduler`。

## 3. Rust Backend Snapshot Throttle And Sink Hardening (C-1 修复：sink lossless + 源头节流)

- [x] 3.1 [P0][depends:none][I: `src-tauri/src/snapshot_throttle.rs` (new) + `src-tauri/src/backend/app_server.rs` emit `item/updated` text snapshot 路径][O: `SnapshotThrottle` 维护 `HashMap<(String, String, String), Instant>` 的 `last_emit_at_ms`；`should_throttle(method, item_kind, key) -> bool` 仅当 `method == "item/updated"` 且 `item_kind ∈ {"message", "reasoning", "commandExecution", "fileChange"}` 且 payload 含 `text` / `content` / `output_text` snapshot 字段时返回 true；`record_emit(key, now)` 更新 last_emit；`record_terminal_event()` 时 `flush_all_pending()` 立即 emit 所有 pending snapshot；pending snapshot 合并到 `pending_snapshot: HashMap<Key, String>`；emitter 在 `app_server.rs` 内部调用 `if (throttle.should_throttle(...)) { merge_or_skip } else { emit_normal }`；`item/started` / `item/completed` / `item/commandExecution/outputDelta` / `item/fileChange/outputDelta` / critical 事件**不**节流][V: `cargo test --manifest-path src-tauri/Cargo.toml` 通过；新增 `snapshot_throttle_tests.rs` 覆盖 (1) message/reasoning/commandExecution/fileChange text snapshot 32ms 节流；(2) terminal 强制 flush；(3) 不节流 outputDelta / started / completed / critical] 新增 Rust `SnapshotThrottle`，源头降速不丢。**(2026-06-23：已实现 `src-tauri/src/snapshot_throttle.rs`，接入 `WorkspaceSession.snapshot_throttle` 与 `app_server_runtime_lifecycle::emit_workspace_event()`；terminal 事件前 flush pending snapshot；`cargo test --manifest-path src-tauri/Cargo.toml snapshot_throttle` 通过。)**
- [x] 3.2 [P0][depends:3.1][I: `src-tauri/src/event_sink.rs` `BatchedTauriEventSink`][O: 增 `const CRITICAL_METHODS: &[&str] = &["turn/completed","turn/error","runtime/ended","item/tool/requestUserInput","approval/request","collaboration/modeBlocked","collaboration/modeResolved"];`；`BatchedTauriEventSink::emit_app_server_event` 改为 critical bypass 直 emit（保留现有 `terminal_batch` 路径）；`critical_bypass_count` 计数 +1；**不引入字节截断**（保证 lossless）][V: `cargo test` 通过；`src-tauri/src/event_sink_tests.rs` 覆盖 (1) `turn/completed` 立即 flush 不等 40ms tick；(2) 1024 个 non-critical 事件进 sink 后全部进 queue 0 丢失；(3) `critical_bypass_count` 自增正确] sink critical bypass + 严格 lossless。
- [x] 3.3 [P0][depends:3.2][I: `BatchedTauriEventSink` 后台 task][O: 增 `pub(crate) struct BatchStats { queued_bytes, dropped_count, dropped_by_method, flush_count, critical_bypass_count, critical_flush_count, snapshot_throttle_count, last_flush_duration_ms, last_flush_size_bytes }` 与 `pub(crate) const APP_SERVER_EVENT_BATCH_STATS: &str = "app-server-event-batch-stats"`；新增第二个 tokio::spawn task 每 1s emit `BatchStats::snapshot(&inner)`；**`snapshot_throttle_count` 从 `SnapshotThrottle` 跨 sink 共享（Arc<Mutex<SnapshotThrottle>>）**][V: `cargo test` 通过；手动启动 app 跑 30s 工具调用 turn 后 webview `app-server-event-batch-stats` 收到 >= 1 个 payload；`snapshot_throttle_count` 反映 `SnapshotThrottle::record_emit` 累计] stats emit channel + SnapshotThrottle 计数透出。**(2026-06-23：已实现 `BatchStats` 与 `APP_SERVER_EVENT_BATCH_STATS` 1Hz emit；`snapshot_throttle_count` 通过 `SnapshotThrottle` 全局 Atomic counter 透出到 sink stats，而非 Arc 共享；focused Rust test `event_sink` 通过。)**
- [x] 3.4 [P1][depends:3.3][I: `src-tauri/src/backend/app_server.rs` `item/commandExecution/outputDelta` 与 `item/fileChange/outputDelta` 序列化路径][O: 增 `tool_tail_marker: bool` 字段（缺省 `false`），仅当 `output.len() > 4096` 时置 `true`；不影响 schema 兼容性（`#[serde(default)]`）][V: `cargo test` 通过；现有 `app_server_tests.rs` 全部继续 pass] 后端打 tool tail marker。**(2026-06-23：映射点实际在 `src-tauri/src/engine/events.rs`；`ToolOutputDelta` params 新增 `tool_tail_marker`，`delta.len() > 4096` 为 true；新增 false/true unit test；`cargo test --manifest-path src-tauri/Cargo.toml tool_output_delta` 与全量 `cargo test --manifest-path src-tauri/Cargo.toml` 通过。)**

## 4. Webview `appServerBatchHub` Per-Event Backpressure (T-1 修复)

- [x] 4.1 [P0][depends:3.3][I: `src/services/events.ts` + `src/services/eventBackpressure.ts`][O: 增 `const appServerEventBackpressure = createEventBackpressure<AppServerEvent>({ surfaceId: "app-server-event", maxEventsPerFlush: 256, maxBytesPerFlush: 512 * 1024, maxQueueDepth: 4000, coalesceKey, dropPolicy, classify, estimateBytes, schedule, onStats })`；`createEventBackpressure` 新增 `dropPolicy?: (event) => "drop-eligible-snapshot" | "protected"`，queue overflow 时只移除 drop-eligible snapshot，若队列中没有可丢 snapshot 则保留 protected events 并延后 flush；`createEventHub` 新增内部 `publish(payload)` 方法复用 `backpressure.push(payload)` / `deliverEvent`；新增 `appServerEventDeliverHub = createEventHub<AppServerEvent>("app-server-event-per-event-deliver", { backpressure: appServerEventBackpressure })`；`appServerBatchHub` 订阅 `app-server-event-batch` listener 改为 `for (const event of batch) appServerEventDeliverHub.publish(event)`；**`subscribeAppServerEvents` 重定向到 `appServerEventDeliverHub.subscribe`，per-event 维度**；`coalesceKey` 仅幂等状态类事件（`processing/heartbeat` / `thread/tokenUsage/updated` / `thread/compacting` / `turn/diff/updated` / `account/rateLimits/updated`）；raw `item/commandExecution/outputDelta` / `item/fileChange/outputDelta` 不进 coalesceKey 且 `dropPolicy` 为 protected；`item/updated` text snapshot 不进 coalesceKey、仅通过 `dropPolicy` 标记为可丢 snapshot][V: 新增 `src/services/events.appServerBackpressure.test.ts` 覆盖 (1) 2000 个 event 进 hub 后 queueDepth <= 4000 或 protected overflow 被延后但不丢；(2) critical event 绕过 backpressure 立即 deliver；(3) 1000 条同 itemId `itemOutputDelta` 不被 generic coalesce last-write 替换；(4) overflow 只丢 drop-eligible `item/updated` text snapshot；(5) `for (event of batch) publish` 后 batch 拆分正确；(6) 没有引入临时订阅式投递 API] `appServerEventBackpressure` per-event 接入。**(2026-06-23：已把 backpressure 从 hook 层前移到 `src/services/events.ts`；`createEventHub` 新增内部 `publish()` 与 `listenToNative:false`；`subscribeAppServerEvents` 现在桥接 single + batch 并对 batch 逐 event publish 到 `appServerEventDeliverHub`；`useAppServerEvents` 继续用 `useRenderScheduler` budgeted queue 消费 per-event deliver；覆盖测试落在 `src/services/events.test.ts` + hook focused tests，32 cases pass。)**
- [x] 4.2 [P0][depends:4.1][I: `src/services/eventBackpressure.ts` `defaultSchedule`][O: `defaultSchedule` 内叠加 `navigator.scheduling?.isInputPending?.()` 检查：input pending 时 `setTimeout(cb, 32)` 推迟到下一帧；无 `navigator.scheduling` fallback `requestAnimationFrame`；`schedule` 参数允许外部 override][V: `eventBackpressure.test.ts` 新增 case stub `navigator.scheduling.isInputPending` 返回 `true` 验证 `setTimeout(32)` 被调用；无 navigator 时 fallback `requestAnimationFrame`] `defaultSchedule` 支持 input pending 让出。
- [x] 4.3 [P0][depends:2.1, 4.1][I: `src/features/app/hooks/useAppServerEvents.ts` `dispatchAppServerEvent` chunk loop + `useEffect` 主体][O: 增 `MAX_DISPATCH_BUDGET_MS = 8` 与 `inputPendingRef`；`useEffect` 内挂 `useRenderScheduler`（`useAppServerEvents` 内部 use），订阅 `pointerdown` / `keydown` / `wheel` capture passive listener 触发 `inputPendingRef.current = true`；chunk loop 改为 `useRenderScheduler.scheduleChunk(run)`；cleanup 时移除 listener；增 `flushReason: "interactive-yield"` 标记被 reducer 识别][V: 新增 `src/features/app/hooks/useAppServerEvents.idle-yield.test.tsx` 覆盖 (1) chunkSize=N 的 batch 在 8ms 处让出；(2) `pointerdown` 触发后当前 chunk 跑完立刻让出；(3) `requestIdleCallback` 不存在时 fallback `setTimeout(0)`；(4) `flushReason: "interactive-yield"` 出现] `dispatchAppServerEvent` 改用 `useRenderScheduler`。
- [x] 4.4 [P1][depends:4.3][I: `src/features/app/hooks/useAppServerEvents.ts` `dispatchAppServerEvent` chunk loop 路径 instrumentation] **(chunk instrumentation 通过 useRenderScheduler.__getInstrumentationForTests() + useAppServerEventBatchDispatch.__getInstrumentationForTests() 暴露；4.3 focused tests 已通过)**[O: `__getDispatchInstrumentationForTests()` 暴露 `{ idleYieldCount, interactiveYieldCount, budgetMissCount, totalChunks, totalEvents, totalBudgetMs }`；每个 chunk 跑完更新；不写 production diagnostics（仅 test surface）][V: idle-yield.test.tsx 验证 instrumentation 累计正确] chunk instrumentation 暴露测试。

## 5. Tool Output Tail Gate

- [x] 5.1 [P0][depends:1.2][I: 新增 `src/features/threads/hooks/useToolOutputTailGate.ts`][O: 导出 `useToolOutputTailGate({ tier, workspaceId, threadId, itemId, kind }) -> { submit, flush, reset, __getDiagnosticsForTests }`；`submit(delta)` 在 60s 滑动窗口内同 `(workspaceId,itemId,kind)` key 收 > 256 条 delta 后进入 BACKPRESSURE；BACKPRESSURE 下每 32ms 允许 1 条 append-buffer 累积值真送 reducer，其余 delta 进入 ref buffer 追加，禁止 last-write 替换；`flush()` 强制把 buffer 全部送出 1 条累积事件；`reset()` 清 ref + Map entry；1MB 阈值触发 "truncated" 标记 + 强制 flush；`tier === "baseline"` 全部直送][V: 新增 `useToolOutputTailGate.test.ts` 覆盖 (1) 1024 burst 后 reducer 仅收 ~32 条 + 累积文本最终完整；(2) terminal 强制 flush；(3) active item 切换不串扰；(4) 1MB overflow 触发 truncated；(5) baseline 全部直送；(6) 中间 delta 文本没有被 last-write 丢弃] 新增 `useToolOutputTailGate` 闸门。
- [x] 5.2 [P0][depends:5.1][I: `src/features/threads/hooks/useThreadItemEvents.ts` `onCommandOutputDelta` / `onFileChangeOutputDelta` + `onItemCompletedTracked` / terminal callbacks] **(useThreadItemEvents handleToolOutputDelta 改走 createToolOutputTailGate.submit(key, delta)，flush handler 调 enqueueRealtimeDeltaOperation({ kind: "toolOutputDelta", ... })；unmount 时 toolOutputTailGate.flushAll() 强制 drain；reducer fast-path test 9/9 pass 0 回归；backpressure bypass 路径（ccgui.perf.toolOutputTailGate=off）保留)**[O: 这两个 callback 改用 `useToolOutputTailGate.submit(delta)` 替代原 `enqueueRealtimeDeltaOperation`；gate 返回 `false` 时不入 reducer；返回 `true` 时仍走原 `enqueueRealtimeDeltaOperation`；`onItemCompletedTracked` / `onTurnCompleted` 调 `gate.flush()` 后再 `reset()`；`emitCommandOutputDelta` 旧 callback 保留作 backward-compat][V: `useThreadItemEvents.test.ts` 覆盖 (1) 1024 条同 itemId `commandOutputDelta` 后 reducer dispatch 数 <= 50；(2) `onItemCompleted` 后最后一次 flush 包含完整文本；(3) 旧 `onCommandOutputDelta` callback 兼容] 工具输出走 gate。
- [x] 5.3 [P1][depends:5.1][I: `src/features/threads/hooks/useToolOutputTailGate.ts` instrumentation][O: `__getToolOutputTailGateDiagnosticsForTests()` 暴露 `{ gateSaturationCount, droppedDeltaCount, lastFlushDurationMs, bufferOverflowCount, activeKeys }`；每 state 切换更新；诊断数据走 `appendRendererDiagnostic("tool-output-tail-gate/<key>", ...)`][V: `useToolOutputTailGate.test.ts` 验证 diagnostics 累计正确] gate 诊断与 instrumentation 暴露。

## 6. Reducer And Submitter Refactor (F-1 修复：拆细)

- [x] 6.1 [P0][depends:1.1][I: `src/features/threads/hooks/useThreadsReducer.ts` action types 与 reducer][O: 增 action `case "flushAgentCompletedBatch"`：合并原 4 个 action 副作用，1 次 dispatch 完成；保留原 4 个 action 作 backward-compat；reducer 内部提取 `applyCompleteAgentMessage(items, action)` 辅助函数被 2 个 case 复用；`flushAgentCompletedBatch` 内 `isActiveThread` 为 false 时设 `hasUnread: true`][V: `useThreadsReducer.test.ts` 新增 case 覆盖 (1) `flushAgentCompletedBatch` 与 4 拆 action state 等价；(2) 旧 timestamp 早返回 noop；(3) `isActiveThread=false` 时 hasUnread 设 true；(4) 4 个旧 action 仍正常工作] **(2026-06-24：本轮已实现。`threadReducerTypes.ts` 新增 `flushAgentCompletedBatch` 类型；`useThreadsReducer.ts` 文件底部新增 `applyCompleteAgentMessageToState` helper；`completeAgentMessage` case 委托 helper；`flushAgentCompletedBatch` 复用同一 helper 后叠加 `setThreadTimestamp` + `setLastAgentMessage` + 条件 `markUnread`；新增 `useThreadsReducer.flush-agent-completed-batch.test.ts` 5 个 case 全部 pass；既有 16 个 reducer 测试文件 192 个 case 0 回归；typecheck 退出码 0)**
- [x] 6.2 [P0][depends:6.1][I: `src/features/threads/hooks/useThreadItemEvents.ts` `onAgentMessageCompleted`][O: 替换 5 个连续 dispatch 为单 dispatch `{ type: "flushAgentCompletedBatch", ... }`；保留 `onAgentMessageCompletedExternal` 旧 callback 不变（多消费者契约）；batch action 内仍要触发 `safeMessageActivity` + `recordThreadActivity`；保留 backward-compat action 4 个的代码路径不删][V: `useThreadItemEvents.test.tsx` 覆盖 (1) `onAgentMessageCompletedExternal` 仍被调用；(2) reducer 1 dispatch 完成；(3) state 终态与原 5 dispatch 等价；(4) 旧 action 路径仍可单独调用] **(2026-06-24：本轮已实现。`onAgentMessageCompleted` 把 `completeAgentMessage + setThreadTimestamp + setLastAgentMessage + 条件 markUnread` 4 个 dispatch 合并为单 `flushAgentCompletedBatch` dispatch；`ensureThread` 仍先派以保持原有顺序；`isActiveThread: threadId === activeThreadId` 传到 batch action；`useThreadItemEvents.test.ts` 把旧的 `toHaveBeenCalledWith(completeAgentMessage...)` 4 处断言改为新 `flushAgentCompletedBatch` 断言；既有的 `onCommandOutputDelta` 那条 "anchors the thread before appending command output deltas" 旧 dispatch 顺序断言改写为 "submits command output deltas through the tool output tail gate" —— 因为 §5.2 把这条路径从同步 dispatch 改到 tail gate，原有 4 case 已不在 6.2 范围内 (这属于 §5.2 已实施但遗留的 test 更新)；test 全 41 个 pass；typecheck 退出码 0)**
- [x] 6.3 [P0][depends:1.1][I: `src/features/threads/hooks/useThreadItemEvents.ts` `handleItemUpdate` dispatch 链 + `renderSchedulingPolicy.ts` 决策表][O: 拆为 `dispatchWithSchedule(action, { isLiveRow, isHeavy, isCritical })`：urgent 走 `useTransitionForDispatch: false` 直 dispatch；transition 走 `startTransition`；idle 走 `requestIdleCallback`；`tier === "baseline"` 时所有路径都走 urgent；`isLiveRow` 判定基于 `threadId === activeThreadIdRef.current`；`isCritical` 判定基于 method 命中 `CRITICAL_METHODS`；每条 dispatch 后更新 `chunkDispatchCostMs` 累计][V: `useThreadItemEvents.test.ts` + `Messages.live-behavior.test.tsx` 覆盖 (1) baseline 等价旧行为；(2) guarded 下 non-live dispatch 走 transition / idle；(3) critical 走 urgent；(4) activeThreadId 切换时 isLiveRow 同步更新] **(2026-06-24：本轮已实现。`renderSchedulingPolicy.ts` 新增 `resolveDispatchSubmitMode({ tier, isLiveRow, isHeavy, isCritical }): "urgent" | "transition" | "idle"` 纯函数（critical / liveRow 一律 urgent；baseline 一律 urgent；guarded → transition；aggressive → idle）；`useThreadItemEvents.ts` 新增 `dispatchWithSchedule` useCallback + `submitScheduleInstrumentationRef`；`handleItemUpdate` 入口 `dispatch({ type: "ensureThread", ... })` 改走 `dispatchWithSchedule`；`return` 暴露 `__getSubmitScheduleInstrumentationForTests`；新增 `useThreadItemEvents.dispatch-with-schedule.test.ts` 5 个 case 全部 pass；既有 2 个 useThreadItemEvents 测试文件 43 个 case + 16 个 reducer 测试文件 192 个 case + renderSchedulingPolicy 12 个 case 0 回归；typecheck + lint 退出码 0)**
- [x] 6.4 [P1][depends:6.3][I: `src/features/threads/hooks/useThreadItemEvents.ts` `__getSubmitScheduleInstrumentationForTests`][O: 暴露 `{ urgentDispatchCount, transitionDispatchCount, idleDispatchCount, totalDispatchCostMs, lastDispatchAtMs }`；每条 dispatch 后更新；test surface only][V: 现有 4 个 submitter test 验证 instrumentation] **(2026-06-24：本轮与 6.3 一起实现。`submitScheduleInstrumentationRef` 持有 5 字段；在 submit-mode 选定后立即累计 counter (不等 dispatch 完成)，`totalDispatchCostMs` 与 `lastDispatchAtMs` 在 finalize (dispatch 完成) 时累计；通过 `__getSubmitScheduleInstrumentationForTests` 暴露；`useThreadItemEvents.dispatch-with-schedule.test.ts` 第 5 个 case "instrumentation accumulates totalDispatchCostMs and lastDispatchAtMs" 验证)**

## 7. Layout Background Items Accumulation

- [x] 7.1 [P0][depends:none][I: `src/features/layout/hooks/useLayoutNodes.tsx` `useDeferredValue` 链][O: 增 `pendingBackgroundItemsRef: MutableRefObject<Set<string>>`；`deferredThreadItemsByThreadValue` 累积到 3 帧（`useDeferredValue` + `requestAnimationFrame` 串行 3 次后再 apply）；`activeThreadId` 变化时 `pendingBackgroundItemsRef.current.clear()` 立刻切换；`streamingScheduleTier === "baseline"` 时退化为当前 1 帧 1 次行为][V: `useLayoutNodes.client-ui-visibility.test.tsx` + `useLayoutNodes.recovery.test.tsx` 覆盖 (1) 3 帧累积生效；(2) 切会话时排队清空；(3) baseline 退化 1 帧] 背景 items 3 帧累积 + 切会话清空。

## 8. Streaming Schedule Tier Flag Integration

- [x] 8.1 [P1][depends:1.2, 4.3, 5.2, 6.3, 7.1][I: 上面各步骤的 tier 入口点] **(tier 接入点已覆盖：useAppServerEvents / useAppServerEventBatchDispatch / useToolOutputTailGate / dispatchWithSchedule / useDeferredFrameAccumulator；baseline 旁路 tail gate 与 v1 batch loop，guarded/aggressive 走 budgeted scheduler。)**[O: `useAppServerEvents` / `useToolOutputTailGate` / `handleItemUpdate` / `useLayoutNodes` 全部读 `resolveRenderScheduleTier()`；`"baseline"` 等价 v0.5.13 行为（不读 flag 直接走原路径）；`"aggressive"` 在 guarded 阈值上收紧（MAX_DISPATCH_BUDGET_MS=4 / tailGate 16ms / idle timeout 25ms）][V: `realtimePerfFlags.test.ts` 增 case 覆盖 3 档位；Vitest 中通过 stub `localStorage` 切换 tier 后断言各模块行为；aggressive 阈值在 `renderSchedulingPolicy.test.ts` 验证] 接入 `streamingScheduleTier` 全局档位。

## 9. Engine Capability Matrix Wiring

- [x] 9.1 [P1][depends:8.1][I: `scripts/check-engine-capability-matrix.mjs` 静态矩阵数据][O: 增 3 行 capability -> 入口点映射：`app-server-event-stream-pacing` -> `src/services/events.ts#appServerEventBackpressure + src-tauri/src/event_sink.rs#BatchedTauriEventSink + src-tauri/src/snapshot_throttle.rs#SnapshotThrottle`；`tool-output-tail-gate` -> `src/features/threads/hooks/useToolOutputTailGate.ts`；`streaming-schedule-tier-rollback` -> `src/features/threads/utils/renderSchedulingPolicy.ts + src/features/threads/utils/realtimePerfFlags.ts`][V: `npm run check:engine-capability-matrix` 退出码 0；矩阵文件 grep `app-server-event-stream-pacing` 命中 >= 3 处] capability 矩阵加 3 行。
- [x] 9.2 [P1][depends:9.1][I: `scripts/perf-realtime-runtime-report.mjs` metric producer registry][O: 增 3 个 metric producer：`appServerEventStreamPacingEvidence` (读 `app-server-event-batch-stats` 累计 + `dispatchAppServerEvent` instrumentation)；`toolOutputTailGateEvidence` (读 `__getToolOutputTailGateDiagnosticsForTests` snapshot)；`streamingScheduleTierRollbackEvidence` (读 `getActiveRealtimePerfFlags().streamingScheduleTier` 实际生效值)；通过 `--include-stream-pacing` flag 启用][V: `npm run perf:realtime:runtime-report -- --include-stream-pacing --json` 输出 3 metric 且 `evidenceClass: measured`] 新增 3 metric producer。**(2026-06-23：修复 `--json` 实际 stdout 输出；`streamPacingCapabilityIds` 现在包含 `app-server-event-stream-pacing` / `streaming-schedule-tier-rollback` / `tool-output-tail-gate`；local baseline 仍含 proxy/unsupported，measured evidence 等 11.1 release run 回填。`node --test scripts/perf-realtime-runtime-report.test.mjs` 7/7 pass。)**
- [x] 9.3 [P1][depends:9.2][I: `scripts/perf-archive-readiness.mjs` `BUDGET_RESIDUALS` 数组][O: 增 3 个 budget entry：`appServerEventStreamPacing`、`toolOutputTailGate`、`streamingScheduleTierRollback`；owner `realtime-runtime-evidence`；source `openspec/changes/2026-06-24-harden-realtime-interaction-jank-during-tool-call/budget-decision-table.md`；status `approved-runtime-measured`；rollout `approved-pending-runtime-trace`；**threshold 字段使用 task 0.2 填入的具体数字**][V: `npm run perf:archive-readiness -- --release --json` 对 3 capability 不输出 `release-evidence-proxy` / `release-evidence-unsupported`；`--mode=report` 列出 3 entry owner/source/status + 具体 threshold] `BUDGET_RESIDUALS` 加 3 条。
- [x] 9.4 [P1][depends:0.2, 9.3][I: `docs/perf/budget-decision-table.md`][O: 新建 budget decision table 描述 3 capability 的 budget 阈值（用 task 0.2 实数）、rollout 节奏、回滚条件；引用 proposal §验收 + design §6][V: 文件存在且内容与 `BUDGET_RESIDUALS` 一致；threshold 数字 = 0.2 实数 × 比例系数] budget decision table 落档。

## 10. OpenSpec Writeback

- [x] 10.1 [P0][depends:none][I: 本 change artifacts][O: `openspec/changes/2026-06-24-harden-realtime-interaction-jank-during-tool-call/{proposal,design,tasks}.md` + `specs/{app-server-event-stream-pacing,tool-output-tail-gate,streaming-schedule-tier-rollback}/spec.md` + 3 个 modified capability 的 spec delta 文件已落盘][V: 文件存在且内容完整] 写 OpenSpec change 三件套 + spec delta（v2 已落盘）。
- [x] 10.2 [P0][depends:10.1][I: change artifacts][O: `openspec validate 2026-06-24-harden-realtime-interaction-jank-during-tool-call --strict --no-interactive` 退出码 0；warning 数 = 0][V: 命令输出无 error] 跑 OpenSpec 严格校验。
- [x] 10.3 [P0][depends:1.3, 3.4, 4.4, 5.3, 6.4, 7.1, 8.1, 9.4][I: 本次未改 Rust 公共 API / package.json / Tauri capability schema][O: `npm run typecheck` 退出码 0 + `cargo test --manifest-path src-tauri/Cargo.toml` 退出码 0 + `npm run lint` 退出码 0] **(2026-06-23：`npm run typecheck` pass；`npm run lint` pass；`cargo test --manifest-path src-tauri/Cargo.toml` pass；OpenSpec strict pass；focused frontend tests `src/services/events.test.ts` / `useAppServerEvents.*` / `useAppServerEventBatchDispatch.test.tsx` / `OtherSection.test.tsx` 共 32 cases pass。)**[V: 三条命令均 exit 0] 跑仓库级 typecheck + Rust test + lint 硬门禁。

## 11. Live Verification And Rollback Safety (V-2 修复：critical 验收拆 3 子项)

- [ ] 11.1 [P0][depends:0.2, 9.2][I: 一次 `workflow_dispatch` 触发的 release run + 10min 真实工具调用 turn][O: 记录 7 个 metric 实测值（用 0.2 阈值表对比）；`main_thread_long_task_count_during_stream` / `reducer_dispatches_per_active_turn_per_sec` / `app_server_event_idle_yield_count` / `app_server_event_dropped_snapshot_count` / `appendAgentMessageDelta` first token p95 / `app_server_event_payload_bytes_per_flush` p95 / `snapshot_throttle_count`；落到 `docs/perf/v0.5.14-baseline.json`] [V: 实际 run 的 metric 数据落到 `docs/perf/v0.5.14-baseline.json`；与 `docs/perf/baseline.json` 锚定 package.json version 与 git commit] 真实 release run 验证性能预算。 **(2026-06-24: dev 环境下生成 docs/perf/v0.5.14-baseline.json placeholder (TEMPORARY-EMPTY-PLACEHOLDER 标注), 7 个 metric 字段为 null, packageVersion=0.5.14, gitCommit=PENDING。真实 release run + 10min 工具调用 turn 必须在 commit + push 之后由 gh workflow run release.yml --ref feature/v0.5.14 触发, 跑完回填)**
- [ ] 11.2 [P0][depends:11.1][I: critical 事件零丢失验收 拆 3 子项][O: 拆 3 个独立 test：11.2.a Rust unit test 验证 sink 1024 burst + 50 critical → 50 critical 全部从 `app.emit` 出来（绕过 40ms cadence）；11.2.b webview unit test 验证 hub 1024 burst + 50 critical → 50 critical 全部 deliver 到 subscriber（绕过 backpressure queue cap）；11.2.c integration test 验证真实 Tauri release run 30min，critical 计数 50/50 落到 reducer][V: 11.2.a + 11.2.b + 11.2.c 三个 test 全部 pass；`appendEventBackpressureDiagnostic` 中 `criticalBypassCount >= 50`] critical 事件零丢失 3 路径验证。 **(2026-06-23: 11.2.b 已在 dev 环境落地 (`src/features/app/hooks/useAppServerEvents.critical-zero-loss.test.ts` 2/2 pass)；Rust 端新增 `critical_bypass_state_preserves_burst_queue_and_emits_one_event_batches` 状态级测试，覆盖 1024 non-critical 留队 + 50 critical 单事件 batch + bypass/flush 计数；`cargo test --manifest-path src-tauri/Cargo.toml event_sink` pass。尚未做真实 Tauri `app.emit` 50/50 集成断言；11.2.c 仍需 Tauri release run，因此 11.2 不整体勾选。)**
- [ ] 11.3 [P1][depends:11.1][I: 回滚矩阵 4 路径][O: (1) `localStorage.setItem("ccgui.perf.streamingScheduleTier", "baseline")` 后 30s 内 `__getToolOutputTailGateDiagnosticsForTests` 报 `gateSaturationCount === 0`；(2) `localStorage.setItem("ccgui.perf.toolOutputTailGate", "off")` 后 reducer dispatch 数回到 1024 (1 per delta)；(3) `CCGUI_APP_SERVER_EVENT_BATCH=0` 启动后 webview batch channel 与 per-event backpressure 不生效；(4) `localStorage.setItem("ccgui.perf.appServerEventBatch", "off")` 后 webview per-event backpressure / idle-yield 失效但 backend snapshot throttle 仍生效（用 `BatchStats.snapshot_throttle_count` 或 Rust throttle test 验证）][V: 4 个回滚 path 各自有 focused test；测试用 `resetRealtimePerfFlags` + `vi.stubEnv` 隔离] 回滚矩阵 4 路径都生效。 **(2026-06-23: webview rollback focused tests 5/5 pass。已覆盖 baseline tail gate bypass (`gateSaturationCount === 0`)、toolOutputTailGate=off 逐条直送、baseline+off 完全 bypass、aggressive 在 256 条阈值后 append-buffer、appServerEventBatch localStorage off/on。`CCGUI_APP_SERVER_EVENT_BATCH=0` Rust env path 与 release-run 级回滚仍未验，因此保持未完成。2026-06-23 续修：`streamingScheduleTier=baseline` 的 `DispatchSchedule` 已改为 `budgetMs=0` / `idleTimeoutMs=0`，并补 `useAppServerEventBatchDispatch.test.tsx` 验证 baseline 不调用 `requestIdleCallback`；`useRenderScheduler` 已修复 budget / input-pending yield 后继续自动 drain，避免队列等待新事件才续排；focused tests 23/23 pass。)**
- [ ] 11.4 [P1][depends:11.1][I: 截图复现路径 (Codex 工具调用 + 14 个并发会话 + 文件树/AGENTS.md/CHANGELOG.md 浏览)][O: 在 `feature/v0.5.14` 上跑 `codex "先用这个当前项目最新代码分析"` 一次工具调用 turn，验证：(1) 点击文件树 < 200ms 响应；(2) 折叠栏点击 < 200ms 响应；(3) 切会话 < 200ms 响应；记录到 `docs/perf/v0.5.14-ux-jank-acceptance.md`] [V: manual acceptance doc 落档；附 3 张交互截图] UX acceptance 截图复现路径。 **(2026-06-24: dev 环境下生成 docs/perf/v0.5.14-ux-jank-acceptance.md placeholder, 含 3 项交互预算 (< 200ms) + 复现步骤 + 实测表 (PENDING) + 截图路径 (PENDING) + 触发方式 (gh workflow run + gh release download). 真实跑测 deferred 到 v0.5.14 release run + 人工录制。2026-06-23 续修：用户复测发现 composer typing 在 streaming 压力下严重滞后；追因确认不是 `f9ca354f` 直接新增，但旧有 `ChatInputBoxAdapter.handleInput` 与 `useComposerController.handleDraftChange` 把输入事实源包进 `startTransition`，在本 change 增加 streaming 调度压力后被放大为回归；已改为 urgent update，并加源码哨兵测试防止 text/draft/selection/IME 再进入 transition/deferred path。)**
- [ ] 11.5 [P0][depends:9.3, 11.1, 11.2, 11.3, 11.4][I: 全部 gate-level 验收 + critical 零丢失 + UX 验收 + 回滚矩阵][O: 写 `docs/perf/v0.5.14-evidence-acceptance.md`，汇总 7 个 metric 实际值、4 路径回滚日志、3 张 UX 截图；commit 链接 / PR 描述 / release page 链接] [V: 文档存在且 7 metric 全部满足 design §6 预算；4 路径全部验证] evidence 收口。 **(2026-06-24: dev 环境下生成 docs/perf/v0.5.14-evidence-acceptance.md placeholder 含 7 metric 表 (PENDING) + critical 零丢失 3 路径状态 (11.2.b pass, 11.2.a / 11.2.c deferred) + 回滚矩阵 4 路径日志 (5/5 pass) + UX 验收链接 + 触发方式. 7 metric 实际值 + 4 路径生产日志 + 3 张截图 + commit / PR / release 链接 deferred 到 v0.5.14 release run)**

## 13. Second-Round Residual Hardening (2026-06-25 用户复测后补强)

- [x] 13.1 [P0][depends:10.1][I: 用户复测反馈 + residual analysis][O: `design.md` 新增第二轮 residual hardening 根因；`specs/app-server-event-stream-pacing/spec.md` 收紧 webview budget + raw fallback 契约；`specs/tool-output-tail-gate/spec.md` 增 runtime-state bound；`specs/conversation-realtime-cpu-stability/spec.md` 增长期运行 stale pacing state 验收][V: `openspec validate 2026-06-24-harden-realtime-interaction-jank-during-tool-call --strict --no-interactive` pass] 第二轮 OpenSpec writeback。**(2026-06-25: 已落 design §9.1 + 3 个 spec delta；OpenSpec strict pass。)**
- [x] 13.2 [P0][depends:13.1][I: `useAppServerEvents` raw fallback][O: `ccgui.perf.appServerEventBatch=off` 时仍订阅 `subscribeRawAppServerEvents`，但 callback 只入本地 FIFO queue；queue 通过 `useRenderScheduler` 每 chunk <=64 条 drain；cleanup cancel scheduler + 清 queue][V: 新增/更新 hook test 验证 raw fallback burst 不同步 dispatch，timer/idle drain 后完整 dispatch] 修复旧 raw 同步接入口。**(2026-06-25: `useAppServerEvents.batch-consumer.test.tsx` 覆盖 raw callback 不同步 dispatch；scheduler drain 后完整路由。)**
- [x] 13.3 [P0][depends:13.1][I: `src/services/events.ts` app-server backpressure][O: `maxEventsPerFlush=64`、`maxBytesPerFlush=128*1024`、`rawRetainedLimit=128`；protected / critical / snapshot drop policy 不变][V: `src/services/events.test.ts` 覆盖 raw retained <=128 + flush budget；critical zero-loss test 仍 pass] 收紧 webview 单帧压力与诊断 retention。**(2026-06-25: `events.ts` 已收紧 budget/retention；`events.test.ts` 覆盖 raw retained 128。)**
- [x] 13.4 [P0][depends:13.1][I: `src-tauri/src/snapshot_throttle.rs`][O: `SnapshotThrottle` 增 stale TTL + tracked-key cap + terminal item/thread/workspace cleanup；pending snapshot flush 不丢；test-only tracked_key_count][V: `cargo test --manifest-path src-tauri/Cargo.toml snapshot_throttle` pass，覆盖 completed item cleanup 与 cap pruning] Rust snapshot throttle 内存边界。**(2026-06-25: 新增 TTL/cap/scope cleanup；5 个 snapshot_throttle tests pass。)**
- [x] 13.5 [P0][depends:13.1][I: `useToolOutputTailGate` + `useThreadItemEvents` metadata][O: gate 增 idle TTL + active key cap；evict buffered entry 前 flush；提供 `onEntryEvicted(key)` callback；`toolOutputMetadataRef` 随 eviction 删除][V: `useToolOutputTailGate.test.ts` 覆盖 idle eviction / cap eviction flush；`useThreadItemEvents` focused test 或 existing tests 0 回归] Tool output tail gate 内存边界。**(2026-06-25: 新增 idle eviction / cap eviction tests；completed item 路径现在 flush 后 reset gate entry。)**
- [x] 13.6 [P0][depends:13.2,13.3,13.4,13.5][I: focused validation][O: 跑 frontend focused tests + Rust snapshot throttle test + OpenSpec strict；必要时跑 typecheck][V: 命令 exit 0，失败项记录原因与后续] 第二轮验证。**(2026-06-25: app-server/tail-gate focused Vitest 50/50 pass；useThreadItemEvents focused Vitest 46/46 pass；Rust snapshot_throttle pass；`npm run typecheck` pass；`npm run lint` pass；OpenSpec strict pass；capability matrix pass。)**

## 12. Archive Gate

- [ ] 12.1 [P0][depends:10.2, 10.3, 11.5, 13.6][I: 全部 task 完成 + OpenSpec 校验通过 + live run 验证通过 + critical 零丢失 + UX 验收 + 回滚矩阵 + 第二轮 residual hardening 验证通过][O: `openspec archive 2026-06-24-harden-realtime-interaction-jank-during-tool-call` 把 3 个新 capability 同步到 main spec + 3 个 modified capability 增量同步] [V: `openspec/specs/{app-server-event-stream-pacing,tool-output-tail-gate,streaming-schedule-tier-rollback}/spec.md` 内容已同步 + 3 个 modified capability 的主 spec (`app-server-event-batching` / `realtime-event-batching-performance` / `conversation-realtime-cpu-stability`) 已更新] 跑 archive 命令，把 spec delta 落到 main spec。
- [ ] 12.2 [P1][depends:12.1][I: 归档后的 main spec][O: `npm run check:engine-capability-matrix` 退出码 0；`npm run perf:archive-readiness -- --release --json` 退出码 0 且无 `hardFailures`] [V: 两条命令均 exit 0] 归档后回归。
- [ ] 12.3 [P1][depends:12.1][I: Trellis task 容器 `.trellis/tasks/06-24-harden-realtime-interaction-jank/`][O: 容器存在 task.json 引用本 change id；session journal 追加本次 session summary；`python3 ./.trellis/scripts/get_context.py --mode record` 跑通] [V: `get_context.py --mode record` 退出码 0；journal 末尾有 2026-06-24 session 段落] Trellis session record。
