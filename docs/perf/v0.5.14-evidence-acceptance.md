# v0.5.14 Evidence Acceptance (Placeholder)

> **Status**: TEMPORARY PLACEHOLDER, awaiting real `feature/v0.5.14` release run + manual UX capture + critical zero-loss 3-path validation.

## 7 个 Gate-level metric 实际值 (待回填)

| Metric | 实测 | 预算 (来源 design §6) | 状态 |
|---|---|---|---|
| `main_thread_long_task_count_during_stream` | PENDING | `<= baseline * 0.6` | TBD |
| `reducer_dispatches_per_active_turn_per_sec` | PENDING | `<= baseline * 0.5` | TBD |
| `app_server_event_idle_yield_count` | PENDING | `>= baseline * 1.2` | TBD |
| `app_server_event_dropped_snapshot_count` | PENDING | `<= 50 / 30min` | TBD |
| `appendAgentMessageDelta_first_token_p95_ms` | PENDING | `<= baseline * 0.8` | TBD |
| `app_server_event_payload_bytes_per_flush_p95` | PENDING | `<= 512 KiB` | TBD |
| `snapshot_throttle_count` | PENDING | `>= 0` (Rust 端启用) | TBD |

baseline 实数见 `docs/perf/v0.5.13-baseline.json` (本提案 0.1 节产出的临时值).

## Critical 零丢失 3 路径

- **11.2.a** Rust 状态级 unit test: `src-tauri/src/event_sink.rs::critical_bypass_state_preserves_burst_queue_and_emits_one_event_batches` (1024 non-critical 留队 + 50 critical 单事件 batch + bypass/flush 计数, `cargo test --manifest-path src-tauri/Cargo.toml event_sink` pass). 真实 Tauri `app.emit` 50/50 集成断言仍 deferred 到 release run。
- **11.2.b** webview unit test: `src/features/app/hooks/useAppServerEvents.critical-zero-loss.test.ts` (本轮已实施, 2/2 pass, 1024 burst + 50 critical → 50 critical 全部 deliver).
- **11.2.c** integration test (30min real Tauri run, deferred to v0.5.14 release run).

## 回滚矩阵 4 路径

`src/features/threads/hooks/__rollback-matrix.test.ts` 5/5 pass:
- path 1: tier=baseline → tool output tail gate bypass, `gateSaturationCount === 0`.
- path 2: toolOutputTailGate=off → handler 同步调 1024 次 (1 per delta).
- path 3: tier=baseline + toolOutputTailGate=off → 100% bypass.
- path 4: appServerEventBatch=off → webview batch consumer 不生效, 走 v1 single-event path.

## UX 验收 (待回填)

详见 `docs/perf/v0.5.14-ux-jank-acceptance.md`.

## 链接 (待回填)

- Commit 链接: PENDING
- PR 描述: PENDING
- Release page: PENDING

## 触发方式

```bash
gh workflow run release.yml --ref feature/v0.5.14
gh run watch # 等待 release build + 测试结束
gh release download v0.5.14 -p '*.dmg'
# 安装后启动, 跑 §11.1 + §11.2.c + §11.4
# 把 7 个 metric 实数 + 4 路径日志 + 3 张截图回填到本文件
```
